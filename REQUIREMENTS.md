This code requires [`docker`](https://www.docker.com/) to be installed and functional.  

If on an ubuntu distribution, set up as follows:
```
apt-get update &&
  apt-get install -y util-linux inotify-tools docker.io git
```
