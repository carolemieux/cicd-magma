=1[\<dñ°°°°°°°°ò<schema xmlns="http://www.asc   <rule context="/*">
         c.net/xml/sqhematron" >
     <pattern name="Test count">
          <rule context="/*">
               <report test="count(//BBB) = count(//AAA)">O.K.</report>
               <assert teB) &lt;= count(//AAA)">Some AAA are missing</assert>
               <report test="count(//BBB) &lt; count(//AAA)">Some BBB are missing</report>
          </rule>
     /pa