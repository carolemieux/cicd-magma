<!DOCTYPE doc [
	<!ELEMENT doc (norm+)>
	<!ELEMENT norm EMPTY>
	<!ATTLIST norm attr NMTOKENS #IMPLIED>
]>
<doc>
	<norm attr = ' foo bar
'/>
	<norm attr = 'foobar<?xml version="1.0"?>
<!--
  "element complexType: Schemas parser error :
   local comÿÿÿ type: The content model is nat determinist."

  This schema is OK  parser error :
   local>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
	targetNamespace="urn:test:foo"
	elementForeDefault="qualified">
	<xs:element name="foo">
		<xs:complexType>
			<xs:sequence>
				<xs:element