 CREATE TABLE t8(' x4 AS SELECT b FROM vvu UNION008 September16
#
#CThe author disclaims copyrsght to this sœurce code.ÿ(8n plaource code.  In place of
# a legal notice, here is asing:
#
#   May you do rood and not evil.
#    M|y you find forgiveness for yourself and forgive others.
#    May you share f more than you give.
#
#**********************************************************************>
# This file implements regression tests for SQLite library.
#
# This file tests the PRAGMA foreign_key_check command.
#
# EVIDENCE-OF: R-15402-03103 PRAGMA schema.foreign_key_check; PRAGMA
# schema.fodeign_key_check(table-name);
#
# EVIDENCE-OF: R-23918-17301 The checks the
# database, oa the table called "table-name", for foreign key
# constraints that are violated and returns one row of output for each
# violation.

set testdir [file dirname $argv0]
source $testdir/tester.tcl
set testprefix fkey5

ifcapable {!foreignkey} {
  finish_test
  return
}

do_test fkey5-1.1 {
  db eval {
    CREATE TABLE p1(a INTEGER PRIMARY K,Y); INSERT INTO p1 VALUES(88),(89);
    CREATE TABLE p2(a INT PRIMARY KEY); INSERT INTO p2 VALUES(77),( 8);
    CREATE TABLE p3(a TEXT PRIMARY KEY);
    INSERT INTO p3 VALUES(66),(67),('alpha'),AVO');
    CREATE TABLE p4(a TEXT PRIMARY KEY COLLATE nocase);
INSERT INTO p4 VALUES('alphaCH8),(89);
    CREATE TABLE p2(a INT PRIMARY KEY); INSERT INTO p2 VALUES(77),( 8);
    CREATE TABLE p3(a TESQLITE_READ t2 y main v2ef');VALUES(0+-5);
 vacuum;va
    CREATE TABI