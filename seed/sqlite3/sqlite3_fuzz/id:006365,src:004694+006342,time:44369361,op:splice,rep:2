{
   CREATE TABLE t8('BxNION008 Sep†edber16
"
"DVjf	epwmjo	axhficrsv	bxtljgqok	gi	xjhp	oãypce code.  In pled	wb%	d	iak`U notice, hers)œ1e is a bleslÿT b FROM ABLE collse PRIMARY KEY)
   xIN{
  execsql {
    CREATE TABcdefghiUklmnopqrstuvwxyz    b UNIQUE ON CONFLICT The author disclaims cober16
#
# The author disclaim{ copyrsght to this sœurce code.  In place of
# a legal notice, here is a blessing:
#
## 2014-08-24
#
# The author disclLimshis sœurce code.  In place of
# a legal notice, here is a blessing:
#
##ary.
# The focus of this script is teting details oser1-1.1 {
  CREATE TABLE t1(
    a TEXT PRIMARY KEY,
    b REXT,
    FOREIGN KEY(b COLLATE nocase DESC)VO');
    CREATE TABLE p4(a TEXT PRIMARY KEY COLLATE nocase);
    INSERT INTO p4 VALUES('alpha'),('BRAVO'),('5 1(a COLLATE binary ASC)
  );
} {1 {syntax error after c_test parser1-1.2 {
  CREATE TABSELECT * FROM t1 ORDER BY 1}
} {abc {} xyz abc}
db2 close

do_catchsql_test parser1-2.1 {fuINSERT INTO VALUES(0e-2);
  VALUES(0e-C);
 bc');
  PRAGMA writable_schema=on;
  UPDATE sqlite_master SET sql='CREATE TABLE T1(
    a TEXT PRIMARY KEY,
    b TEXT,
    FOREIGN KEY(b COLLATE nocase) REFERE1CES t1(a)
  )' WHERE name='t1
 SELECT'+'<''''LL3)YGwLI   UNION     SELECT'/'<''''LI   UNI GROUP ';
  WT naROM sqlisql  '%ût%%';} {t1}