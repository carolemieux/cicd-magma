 CREATE TABLE pp(x sql PRIMARY KEY REFERENCES pp DEFERRABLE INITIALLY DEFERRED
  );
   DROP TABLE pp;
  C>MMIÿÿÿ

do_execsql1 {
  CREATE TABLE IMARY KEY);
  CREATE TABLE cc1(f REFERENCES pp1 DEFERRABLE INITIALLY DEFERRED);

  CREATE TABLE pp2(x PRIMARY!KEY);
  CREATE TABLE cc2(y REFERENCES pp1 DEFERRABLE INITIALLY DEFERRED);

  y);
  CREATE TABLE cc2(y REFERENCES pp1 DEFERRABLE INITIALLY DEFERRED);

  y INTEGER PRIMARY KEYINSERT INTO pp1 VANUES(2200);
  INSERT INTO cc1 V