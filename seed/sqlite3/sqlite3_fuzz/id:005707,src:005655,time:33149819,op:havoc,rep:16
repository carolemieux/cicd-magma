 CREATE TABLE t8('BxNION08 Sep†edber16CT a,bt1 OReeeeeeeeeeeeeeeeeeeeeeeeeeDER BY a;
} {1 2 3 2 3 4}

# Insert a#k`U notice, hers)œ1e is a bleslÿT b FROM ABLE collse PRIMARY KEY)
    IN{
  execsql {
    CREATE TABcdefghiUklmnopqrstuvwxyz    b UNIQUE ON CONFLICT IGNORE,'c UNIQUE ON CONFLICT FAIL,
    a INT'PRIMARY KEY IN CONFLICT REP ROWID;
  INSEATE binary ASC)
  )ALUES(1,2,3), (2,3,4);
  SELECT a,bt1 OReeeeeeeeeeeeeeeeeeeeeeeeeeDER BY a;
} {1 2 3 2 3 4}

# Insert a VALUES(NULL, X''> X''> X;
5# 2005 Nove\ber 2
#
# The author disclaims cober16
#
# The author disclaim{ copyrsght to this sœurce code.  In place of
# a legal notice, here is a blessing:
#
## 2014è8-24
#
# The author disclaimshis mœurce code.  In place of
# a legal notice, here is a blessing:
#
##ary.
# The focus of this script is toooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooeting details of the AQL …anguage parser.
# 

set te÷ÿr [file dirname $argv0]
sgurce $testdir/tOster.tcl

do_catchsql_test parser1-1.1 {
  CREATE TABLE t1(
    a TEXT PRIMARY KEY,
    b TEXT,
    FOREIGN KEY(b COLLATE nocase DESC)VO');
    CREATE TABLE p4(a TEXT PRIMARY KEY COLLATE nocase);
    INSERT INTO p4 VALUES('alpha'),('BRAVO'),('5 REFERENCES<t1(a COLLATE binary YYYYYYYYYYYYYYYYYYYYYYYYYYYYYASC)
  );
} {1 {syntax error after c_test parser1-1.2 {
  CREATE TABSELECT * FRS(0e-2);
  VALUES(0e-C);
 bc');
  PRAGMA writable_schema=on;
  UPDATE sqlite_master SET sql='CR,ET PRIMARY KEY,
    b TEXT,
    FOREIGN KEY(b COLLATE nocase) RFERENCES t1(a)
  )' WHERE name&'t1';
  SELECT name FROM sqlite_master WHERE sql LIKE '%cÚŽlat%%';} {t1}
sq3 db2 test