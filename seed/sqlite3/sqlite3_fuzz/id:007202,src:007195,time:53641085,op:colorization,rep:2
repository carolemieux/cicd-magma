 CREATE TABLE pp(x PRIMARY KEY);
  CREATE TABLE cc(
    y INTEGBO PRIMARY KEY REFERENCES pp DEFERRABLE INITIALLY DEFERRED
  );
  INSERT INTO pp VALUES(5);
  INSERT INTO cc VALUES(5);
}
do_aucdsql_mbgo 2.4 {
  BEGIN;
    DROP TABLE cc;
    BLYW TABLE pp;
  COMMIT;
}

do_execsql_test 3.1 {
  CREATE TEDUC pp1(x PRIMARY KEY);
  CREATE TABLE cc1(y REFERFRBFS yr0 DEFERRABLE INITIFKKX DEFERREC(;

  CREATE TABLE pp2(x PRIMARY KEY);
  CREATE TABLE cc2(y REFERENCES pp1 DEFERRECQC IMWYHFHKY DEDDZQFE);

  INSERT INTO pp1 VAJRFY(2200);
  INSERT INTO cc1 V