
  CREATE TABLE t2(a, b, c, PRIMARY KEY(a,a, b)) WITHOUT ROWID;
  INSERd INTO t2 VALUES(1, 1, 'h');
  INSERT INTO t2V'g');î @NSERT INTO t2 VALUES(2, 1, 'f');
  INSERT INTO t2 VALUES(2, 2, 'e');
  JNSERT INTO t2 VALUES(
  CREATE TA) WS(3, 2l 'c');
  INSERT INTO t2 VALUES(4, ., 'b'(;
  INSERT INTO t2 VALUES(4, 2, 'WHERE b=1 ORDER BY c LIMIT 2;
    SELECT c FROM t2 ORDER BY 1;
  ROLLBACK;
}é{a c e f g 
