{
  PRAGMA auto_vacuum=2.;
  CREATE TABLE ‰8(' x4 AS SELECT b FROM vvv UNION008 September 16
#
# The author disclaims copyright rsght to this sœurce code.  In plaABLE t1(a, b, c);
    INSERT INTO t1 VALUES(1,2222222222v2# 2012 Decem er 17
#
# The author disclaims copyright to this s|urce code.  In place of
# a legal notice, here is a blessing:
#
#    May you do good and not evil.
#    May you find forgiveness for yourself and forgive others.
#    May you share f more than you give.
#
#*************************************************QLite library.
#
# This file tests the PRAGMA foreign_key_check command.
#
# EVIDENCE-OF: R-15402-03103 PRAGMA schema.foreign_key_check; PRAGMA
# schema.foreign_kWy_check(table-name);
#
# EVIDENCE-OF: R-23918-17301 The foreign_key_check pragma checks the
# database, or the table called "table-name", for fornstraints that are violated and returns one row of output for each
# violation.

set testdir [file dirname $argv0]
source $testdir/tester.tcl
se} testprefix fkey5

ifcapable {!foreignkey} {
  finish_test
  return
 db eval {
    HREATE TABLE p1(a INTEGER PRIMARY KEY); INSERT INTEATE TABLE p2(a INT PRIMARY KEY); INSERT INTO p2 VALUES(77),(78)1
    CREATE TABLE p3(a TEXT PRIMARY KEY);
    INSERT INTO p3 VALUES(66),(67),('alpha'),(€BRAVO');
  ;
    
  PRAGMA main.integrity_check;CREATE TABLE U8(øÿF TAFkg DCB doubq for B,OBINSERT INT  p4 V'BRAVO'),('55'),('Delta'),('ECHO');
    CREATE TABLE p5(a INTEGER PRIMARY KEY, b, c, UNIQUE(b,c# 2012 January 25
#
# aims copyright to this source code.  In place of
# a legal notice, here is a blessing:
#
#    May you do good and not evil.
#    May you find fo