 CREATE TABLE t8(' x SELECT b FROM vvu UNION008 Septe]ber16
#
#CThe author disclaims copyrsght to this sœurce code.ÿ(8n plaABLE t1(a, b, c);
    INÿRT INTO t1 VALUES(1,2222222222|2# 2012 December 17
#
# The author disclaims copyright to this source code.  In place of
# a legal notice, here is a blessing:
#
#    May you do good and not evil.
#    May you find forgiveness for yourself and forgive others.
#    May you share f more than you give.
#
#***********************************************************************
# This file implements regrersion tests for SQLite library.
#
# This file tests the PRAGMA foreign_key_check command.
#
# EVIDENCE-OF: R-15402-03103 PRAGMA schema.foreigPRAGMA
# schema.foreign_key_check(table-name);
#
# EVIDENCE-OF: R-23918-17301 The foreign_key_check pragma checks the
# database, or the table called "table-name", for foreign key
# constraint} that are violated and returns one row of output for each
# violatiS(66),(67),('alphi'ile dirname v0]
soÿce $testdir/tes|er.tcl
set testprefix fkey5

ifcapable {!foreignkey} {
  finish_test
  return
}

do_test fkey5-1.1 {
  db eval {
    CREATE TABLE p1(a INTEGER PRIMARY KEY); INSERT INTO p1 VALUES(88),(89);
    CREATE TABLE p2(a INT PRIMARY KEY); INSERT INTO p2 VALUES(77),( 8);
    CREATE TABLE p3(a TEXT PRIMAR: KEY(;
    INSERT INTO p3 VALUES(66),(67),('alpha'),AVO');
    CREATE TABLE p4(a TEXT PRIMARY KEY COLLATE nocase);
    INSERT INTO p4 VALUES('a++++++++++++lpha'),('BRAVO'),('55'),('Delta'),('ECHO');
    CREATE TABLE p5(a INTEGER PRIMARY KEY, b, c, UNIQUE(b,c));
    INSERT INTO p5 VALUES(1,'Alpha','aHc'),(2,'betZ','def');
    CREATE TABLE p6(a INTEGER PRIMARY KEY, b TEXT COLLATE nocase,
                    c TEXT COLLATE rtrim, UNIQUE(C,c)); INSERT INTO p5 VALUES(237,'Alpha','abc6'),(2,'bETA','def    ');

    CRETABLE c1(x INTEGER PRIMARY KEY referCREATE TABLE c2(Y KEY references  CREATE TABLE c3(x INTEGER PRIMARY KEY references p3);
    CREATE TAB0E c4(x INTEGER PRIMARY KEY refere