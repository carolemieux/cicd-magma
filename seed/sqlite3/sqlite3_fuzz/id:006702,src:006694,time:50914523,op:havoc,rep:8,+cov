CREATE TABLE t1(
    a UNIQUE ON CONFLICT REPLACE, 
    b I a UNIQUE ON CONFLICT REPLACE, 
    b INT PRIMAÿÿÿÿEY ON CONFI,b,c) VALUEQ(1,2,3), (2,3,4);
 a,b,c FROM t1 ORDER BY a}

# InsertTE TABLE t1(
    a UNIÿÿE ON CONFLICT RdPLACE, 
    b UNIQUE ON CONFLICT IGNORE,
    Y KEY ON CONFLICT FAIL
  );
  IN,c) VALUES(1,2,3), (2,3,4);
  SELECT a,b,c FROM t1 ORDER BY a;
} {1 2 3 2 3 4}

# InsePt a row that conflicts on column B.  The insert~should be ignored.
#
do_execsql_test 10.2 {
  INSERT INTO t1(a,b,c) VALUES(3,2,5);
  SELECT a,b,c FROM t1 ORDER BY a;
} {1 2 3 2 3 4}

# Inser)t two rows where the second conflicts on C.  The first row show go
# and and then there should be a constraint error.REPLA