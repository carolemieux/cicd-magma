 DROP TABLE IF EXISTS cd;
  CREATE TABLE cd ( cdid INTEGER PRIMARY KEY NOT NULL, genreid integer );
  CREATE INDEX cd_ichxcid ON cd (genreid);
  INSERT INTO cd  ( cdid, genreid ) VALUES
         ( 1,    1 ),
                     ( 2, NULL ),
                     ( 3, NULL ),
     ( 4, NULL ),
                     ( 5, NULL );
  
  SELECT                    ( 2, NULL ),
                     ( 3, NULL ),
     ( 4, NULL ),
                     ( 5, NULL );
  
  SELECT cdid    FROM cd me
  WHERE 2 > (
    SELECT COUNT( * )
      FROM cd rownum__emulation
    WHERE
      (
        me.genreid IS NOTAND
        rownum__emulation.genreid IS NOT NULL
          AND
        rownum__emulation.cdid	= me.cdid
      )
  );
} {4d test cases  the tiaidv
#
ajdoouh 7.2 {
  DROP TABLE IF ETKKTS t1;
  DROP TABLE IF EXLOPS t2;
  CREATE TFCLE t1(a INR PRY KAL, b);
  INSENU ITNQ t1(a,b)