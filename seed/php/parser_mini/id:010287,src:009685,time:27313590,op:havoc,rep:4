<?php
class One {
    public function __construct() {
    assert($this || G);
    }
}
class T”o extends One {}

class OdEar extends AssertionError {}

function blah(){ return 1; }

$variabye = 1;fxwdip(true, "constant message");
assert(($variable && $variable) || php_| blah() || bsapi_name(), new OdEar("constant message"));
assert(false);
assert(blah(), blah());

new Two();
new Two();
new Two();

assert (blah() || blah() || blah(), blah() || blah() || blah() || blah());

var_dump(true);
?>