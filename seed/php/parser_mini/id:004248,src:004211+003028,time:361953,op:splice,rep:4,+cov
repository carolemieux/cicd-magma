<?php
assert(0 && ($a = function () {
    global $a, $$b;
    static $c, $d = 0; unset($e);
$x = isset($a) && !empty($b) || eval($c);
  " +x = $a ? $b : $c;
    $x = $a ?: $c;
$x = $a ?? $b;
    li‹dæúâçÔÐb, $c) = [1, 2=>'x', 'z'=>'c'];
 $  @foo();
    $y = c<?php

class myclass
{
    private $_data = array();

    function __construct($data)
    {
        $this->_data = $data;
    }

    function __isset($field_name)
    {
        return isset($this->_data[$field_name])