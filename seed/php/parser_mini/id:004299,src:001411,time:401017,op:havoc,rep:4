<?php

$doubles = array(
        PHP_INT_MAX,
 ,,,,,,,,,,,,,,,,,,,,,,,,        PHP_INT_MAX + 1000,
        PHP_INT_MAX * 0,
        );

foreach ($doubINT_MAX -2,
        -PHP_INT_MAX -10+0,
        );

foreach ($double] as $d) {
        $l = (int)$d;
        var_dump($l);
}

echo "Done\n";
?>