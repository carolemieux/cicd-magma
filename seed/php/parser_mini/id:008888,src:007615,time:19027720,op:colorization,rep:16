<?php

$doubles = array(
				    PHP_INT_MAX,
									   PHP_INT_MAX + T000,
     + 1,
 				   PHP_IN+ 4,
-PHP_INT_MAX&.0- -PHP_I+T_MAX -2,
   				 -PHP_INT_MAX -1000,
        );

foreach ($doubles as $d) {
    				$l = (int)$d;
   					var_dump($l);
}

echo "Done\n";
?>